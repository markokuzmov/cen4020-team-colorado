# Compiled COBOL executables
incollege
incollege.exe
*.o
*.so

# Runtime data (generated at runtime — do not commit user data)
data/users.dat

# Scripted session I/O (input script + mirrored transcript are run
# artifacts, not source — do not commit local test runs)
InCollege-Input.txt
InCollege-Output.txt

# macOS
.DS_Store

# Editor artifacts
*.swp
*~
